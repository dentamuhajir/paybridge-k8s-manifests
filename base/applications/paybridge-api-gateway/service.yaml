apiVersion: v1
kind: Service
metadata:
  name: paybridge-api-gateway
  namespace: paybridge
spec:
  selector:
    app: paybridge-api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP